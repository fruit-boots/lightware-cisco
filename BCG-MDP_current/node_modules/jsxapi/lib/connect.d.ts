import { InitBackend, Options } from './types';
import XAPI from './xapi';
export declare const globalDefaults: Options;
export interface Connect {
    (options: Partial<Options>): XAPI;
    (url: string, options?: Partial<Options>): XAPI;
}
export default function connectOverload(initBackend: InitBackend, defaults: Partial<Options>): Connect;
